{
  "name": "go-shortlinks",
  "version": "1.0.0",
  "description": "Fetches shortlinks from Payload CMS and generates Netlify _redirects file.",
  "main": "fetch-redirects.js",
  "scripts": {
    "build": "node fetch-redirects.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.0.0"
  }
}
